package co.tiagoaguiar.tutorial.jokerappdev.model

import androidx.annotation.VisibleForTesting

data class Category(
    val name: String,
    val bgColor: Long // desta forma podemos inserir as cores em dexacimal
)
